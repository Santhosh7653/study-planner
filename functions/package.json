{
  "name": "study-planner-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Study Planner reminders",
  "main": "src/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "googleapis": "^140.0.0",
    "nodemailer": "^6.9.0"
  }
}
